# Screenshots

Drop PNGs here, named to match the README image links:

| File | Capture |
|---|---|
| `01-dashboard.png` | Main workspace after an assessment completes (intake + pipeline + brief). |
| `02-pipeline-degraded.png` | Pipeline mid-run, or a run with a ⚠ Partial Data warning on a degraded source. |
| `03-risk-brief.png` | Risk brief panel: gauge, confidence, decision, expanded market signals / policy hits. |
| `04-dynatrace-genai.png` | A `procureiq-backend` trace in Dynatrace showing `gen_ai.*` span attributes (model, tokens). |
| `05-ci-green.png` | GitHub Actions run with `lint-test → build → deploy` all green. |

Tip: crop to content, keep widths ~1200–1600px. Use a light theme for the app shots so they read well in the README.
